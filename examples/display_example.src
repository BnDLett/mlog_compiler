def main() {
    // Variable retrieval
    var item_amount = sense(container1, totalItems);
    var size = item_amount / 4.29;

    // Control display
    clear(0, 0, 0);
    rectangle(5, 5, size, 10);
    update(1);

    // Message blocks
    print("Total Items: ", 1, false);
    print(item_amount, 1);

    print("Created by: [#003ec8]BnDLett", 2);
}
