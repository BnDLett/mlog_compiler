from setuptools import setup

setup(
    name='mlog-compiler',
    py_scipts=['mlog_compiler'],
    version='1.1.0',  # This won't be changed until the rework is being pushed into the master branch.
    description='A compiler that compiles a custom-syntax language into Mindustry logic.',
    author='Lett',
    install_requires=[

    ],
)
